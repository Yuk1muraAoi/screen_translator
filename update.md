## [Unreleased] 记忆管理系统与多项目翻译

### 新增功能

#### 1. 翻译项目管理
- **多项目支持**：用户可以像聊天机器人一样创建多个独立的翻译项目，每个项目相互隔离。
- **项目命名**：新建项目默认名称为当前时间格式 `{yyyy}-{mm}-{dd}-{hh}-{mm}-{ss}`，支持用户随时修改。
- **临时翻译项目**：置顶一个特殊的「临时翻译」项目。该项目中的翻译不带上下文，且翻译结果在应用关闭或下次启动时自动清空。

#### 2. 持久化存储
- 截图与翻译结果保存至本地长期存储，应用重启后历史记录不丢失。

- 保存的项目直接放在当前文件夹的`history`文件夹下即可，每个项目单独设置文件夹，文件夹名称是项目名称。

- 保存项目时使用json文件保存翻译结果，按照以下格式存储翻译结果：

  ```json
  [
      {
          "round": 0,
          "image_url": "",
          "translation": "{{system_prompt}}",
      },
      {
      	"round": 1,
          "image_url": "{{image_path}}",
          "translation": "{{llm_translation_result}}"
      },
      ...
  ]
  ```

  图片也放在项目文件夹下，不需要使用SQL之类的数据库框架。

- 每个项目独立维护自身的历史翻译记录。

#### 3. 上下文感知翻译
- 在同一个翻译项目下，之前的翻译结果会作为上下文随新截图一起发送给模型（只发送结果不要发送截图），实现连贯的「带上下文翻译」。
- 不同项目之间的上下文完全隔离。
- 注意，之前我让你实现用户可以自行修改翻译结果的功能现在派上用场，如果LLM翻译结果是错的、或者用户想修改其中的人名地名等专有名词的翻译，那么修改了翻译结果之后的内容作为上下文，在下一轮翻译时发送给模型会提高翻译的准确性，所以你在保存翻译结果的时候一定记得及时更新用户。

#### 4. 项目级自定义提示词
- 每个翻译项目均可独立设置系统提示词（System Prompt），默认使用全局的提示词，用户可以自行修改。
- 「临时翻译」项目同样支持独立提示词设置。

#### 5. 项目管理界面（前端）
- 新增直观的翻译项目侧边栏/列表视图，符合人类使用直觉。
- **右键菜单**：右键单击任意项目（除「临时翻译」项目外）可弹出菜单，支持：
  - 修改项目名称
  - 修改项目提示词
  - 删除项目
- 界面布局与现有浮窗风格保持一致。
